# DevAgency Pro — IT-Outsourcing Landing Page

## 📋 Быстрый старт

### Локальный сервер
```bash
python server.py
# Откройте http://localhost:8000
```

---

## 📧 Настройка отправки заявок

### Опция 1: Formspree (рекомендуется для продакшена)

1. **Зарегистрируйтесь** на https://formspree.io (бесплатно)
2. **Создайте новую форму** с вашей рабочей почтой (например, info@devagency.pro)
3. **Получите Form ID** (выглядит как `fXXXXXXXX`)
4. **Отредактируйте `script.js`** (строка 13):
   ```javascript
   const FORMSPREE_FORM_ID = 'fXXXXXXXX'; // Замените на свой ID
   ```
5. **Сохраните** и готово! ✓

**Преимущества Formspree:**
- ✓ Заявки приходят прямо на вашу почту
- ✓ Спам-фильтр встроен
- ✓ Бесплатно (50 заявок/месяц на базовом плане)
- ✓ Интеграция с Telegram, Slack, Webhook

### Опция 2: localStorage (для демонстрации)

Текущий режим работает с localStorage. Заявки сохраняются локально в браузере. Для просмотра откройте **DevTools** (F12) и выполните:

```javascript
JSON.parse(localStorage.getItem('applications'))
```

---

## 🎯 Функциональность

### Калькулятор стоимости
- Выбор услуги (веб, мобиль, поддержка, облако)
- Выбор сроков (1-3мес, 3-6мес, 6+мес)
- Выбор сложности (базовая, стандартная, сложная)
- Автоматический расчёт стоимости

**Формула:** `Базовая стоимость × Множитель сроков × Множитель сложности`

### Форма заявки
- Валидация: email, телефон, описание (мин. 10 символов)
- Отправка на Formspree или сохранение в localStorage
- Успех/ошибка сообщения с автоочисткой

### Отзывчивость
- ✓ Мобильные (до 768px)
- ✓ Планшеты (768px-1024px)  
- ✓ Десктопы (1024px+)

### Dark Mode
Для включения добавьте в HTML (или консоль DevTools):
```javascript
document.documentElement.setAttribute('data-theme', 'dark');
```

---

## 📁 Структура проекта

```
spec-generator/
├── index.html          # 8-секционный лендинг
├── style.css           # Стили + dark theme
├── script.js           # Калькулятор + обработка форм
├── server.py           # Локальный HTTP сервер
├── package.json        # npm зависимости
└── README.md          # Этот файл
```

---

## 🚀 Развёртывание

### На Vercel (бесплатно, рекомендуется)
```bash
# Установите Vercel CLI
npm install -g vercel

# Развёртывание
vercel
```

### На Netlify
1. Свяжите GitHub репозиторий
2. Выберите папку `spec-generator/`
3. Готово!

### На собственном сервере
```bash
# Скопируйте файлы на сервер
# Настройте веб-сервер (Nginx, Apache)
# HTTPS обязателен для Formspree!
```

---

## 🛠 Технологический стек

- **HTML5** + **Bootstrap 5** (grid, responsive)
- **CSS3** (градиенты, анимации, dark theme)
- **JavaScript** (vanilla, no dependencies)
- **FontAwesome 6.4** (иконки)
- **Formspree** (опционально, для почты)

---

## 📝 Изменение контента

### Добавить услугу
1. Отредактируйте [index.html](index.html#L63) (секция `#services`)
2. Обновите базовую стоимость в [script.js](script.js#L26) (`baseCosts` объект)

### Изменить цвета
В [style.css](style.css#L8) обновите `:root`:
```css
:root {
    --accent-color: #YOUR_COLOR;
    --button-color: #YOUR_COLOR;
}
```

### Добавить секцию
Копируйте существующую секцию, обновляйте ID, классы и контент.

---

## 🐛 Отладка

### Консоль браузера (F12)
```javascript
// Проверить сохранённые заявки
JSON.parse(localStorage.getItem('applications'))

// Очистить хранилище
localStorage.clear()

// Проверить статус Formspree
// Сообщения в консоли при ошибках
```

### Проверить Formspree
Посетите https://formspree.io → ваш проект → посмотрите историю отправок

---

## 📞 Контакты в коде

Обновите в [index.html](index.html#L396):
- **Телефон** (строка ~407)
- **Email** (строка ~412)
- **Адрес** (строка ~400)
- **Социальные сети** (строка ~414-418)

---

## 📄 Лицензия

MIT — используйте как угодно!

---

**Создано:** 29.12.2025  
**Версия:** 1.0.0
